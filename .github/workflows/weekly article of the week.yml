name: weekly article of the week

on:
  schedule:
    - cron:  '0 0 * * 1' #every monday at 00:00
  workflow_dispatch:

jobs:
  bots:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package
    - name: Run bot
      env:
        BOT_DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        BOT_PFKEY: ${{ secrets.PFKEY }}
        BOT_SFKEY: ${{ secrets.SFKEY }}
        BOT_ANTI_PROTECTION_SECRET: ${{ secrets.ANTI_PROTECTION_SECRET }}
      run: java -jar bots/target/bots-0.0.0.jar article-of-the-week